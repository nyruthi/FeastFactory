services:
  - type: web
    name: feast-factory-frontend
    env: node
    buildCommand: npm run build
    startCommand: npx serve -s dist -l 3000
    envVars:
      - key: NODE_ENV
        value: production
