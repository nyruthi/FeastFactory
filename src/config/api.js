// Update BACKEND_IP with your PC's local IP (run `ipconfig` on Windows)
// For production, replace with your deployed backend URL
export const BACKEND_IP = '192.168.88.11';
export const BACKEND_URL = `http://${BACKEND_IP}:3000`;
